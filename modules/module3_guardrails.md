Functions
ValidateSections(canonical_sections):

Flag missing/empty/<50-word sections.

PreventHallucination(summary, section_text):

Strip unverifiable claims; mark “Not stated”.

ApplyChunkingPolicy(section_text):

Ensure sub-summaries precede aggregation.

EnforceTerminology(summary, registry):

Stabilize terms and acronyms.

PreserveOrder(section_order, outputs):

Lock output sequence.

Key outputs
validated_summaries

consolidated_flags

updated_registry
