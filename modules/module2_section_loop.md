Functions
ChunkSectionText(section_text):

Segment for long sections; maintain references.

SummarizeSection(chunks, limit=120):

Produce concise bullets or paragraph.

EvidenceCheck(summary, section_text):

Verify traceability; tag “Not stated” where ambiguous.

AcronymManager(summary, registry):

Expand once; enforce reuse.

Key outputs
section_summaries

evidence_notes

flags per section
