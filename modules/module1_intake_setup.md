Functions
NormalizeSectionTitles(section_list, text):

Map variants to canonical names.

DetectStructure(text):

Split by headings; align to section_list order.

IdentifyAudience(audience_flag):

Set expert/lay parameters.

PrepareVariables():

Initialize limits, registries, evidence maps, flags.

Key outputs
canonical_sections

section_order

issues: missing, empty, short

config: limits, audience, registry
